// @flow
import type { HierarchyRowRecord } from ".";
import type { State } from "state";

export const deleteHierarchies = (state: State, row: HierarchyRowRecord): State => {
    return state
}

export const withoutRow = (state: State, hierarchyIndex: number, rowIndex: number): State => {
    return state
}